{
  "name": "node-http2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "genCert": "openssl req -new -newkey rsa:2048 -sha256 -days 365 -nodes -x509 -keyout server.key -out server.crt"
  },
  "license": "MIT",
  "dependencies": {
    "fastify": "^2.15.1"
  }
}
